[package]
name = "zero-log"
version = "0.2.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = {version = "0.8.2",features=["html_reports"]}

[[bench]]
name = "log_benchmarks"
harness = false
